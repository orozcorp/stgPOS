{
  "name": "stgpos",
  "version": "1.0.0",
  "description": "POS for sterling",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "concurrently \"cd stg-pos-nxt && yarn next dev\" \"yarn start\"",
    "next-dev": "cd stg-pos-nxt && yarn next dev",
    "next-build": "cd stg-pos-nxt && yarn build && yarn export",
    "build": "yarn next-build && electron-builder"
  },
  "author": "Orozcorp",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "electron": "^28.1.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "electron-is-dev": "^2.0.0",
    "mongodb": "^6.3.0"
  }
}
